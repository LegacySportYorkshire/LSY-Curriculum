import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "uk.co.legacysportyorkshire.curriculum",
  appName: "LSY Curriculum",
  webDir: "out",
};

export default config;